spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:mydb;NON_KEYWORDS=VALUE
    username: sa
    password: password
    driverClassName: org.h2.Driver
    hikari:
      maximum-pool-size: 3

user-credentials:
  name: persapiens
  password: account
  authorities: ROLE_USER
